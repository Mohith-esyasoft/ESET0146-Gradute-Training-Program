function CalculationsDisplay({ sum, largestNumber }) {
  return (
    <div>
      <h2>Calculations:</h2>
      <p>Sum of all numbers: {sum}</p>
      <p>Largest number: {largestNumber}</p>
    </div>
  );
}

export default CalculationsDisplay;
